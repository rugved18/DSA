#include<bits/stdc++.h>
using namespace std;

int duplicate_removal(vector<int> arr, int n){
    int i=0;
    int j=n-1;

    if(arr[i] != arr[j]){
        i++;
        j--;
    }    
}

int main(){
    int n;
    cin >> n;
    vector<int> arr(n);
}